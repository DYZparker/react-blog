import styled from 'styled-components'

export const SiderWrapper = styled.div`
  margin-left: 1.7rem;
`

export const AutorWrapper = styled.div`
  text-align: center;
  border-radius: .3rem;
  background-color: #fff;
  box-shadow: 0 1px 3px rgba(26,26,26,.1);
  padding: 1.7rem;
  overflow: hidden;
`

export const AutorContent = styled.div`
  margin-top: 2rem;
  p {
    font-size: .8rem;
  }
`

export const AutorAccount = styled.div`
  text-align: left;
  font-size: 1.2rem;
  margin: .5rem 0;
  span {
    font-size: .7rem;
  }
  a:hover {
    color: #f56a6a;
  }
`

export const TagsWrapper = styled.div`
  border-radius: .3rem;
  background-color: #fff;
  margin-top: 1.7rem;
  box-shadow: 0 1px 3px rgba(26,26,26,.1);
  padding: .5rem;
  overflow: hidden;
`

